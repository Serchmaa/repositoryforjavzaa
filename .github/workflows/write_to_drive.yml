name: Write to Google Drive

on:
  schedule:
    - cron: '*/1 * * * *'  # Every 30 minutes

jobs:
  write-to-drive:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Print message
      run: echo "This is a test to confirm the workflow syntax is correct."
